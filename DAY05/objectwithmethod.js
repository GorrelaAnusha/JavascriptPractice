const calculator = {
  add: function(a, b) {
    return a + b;
  },
  multiply(a, b) {
    return a * b; // shorthand method
  }
};

console.log(calculator.add(5, 3));      // 8
console.log(calculator.multiply(4, 2)); // 8
