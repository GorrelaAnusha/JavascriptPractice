const user = { id: 101, username: "anusha", email: "anu@example.com" };

const { id, username } = user;

console.log(id);       // 101
console.log(username); // anusha
console.log(user.email);    // anu@example.com